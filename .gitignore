# Dependencies
node_modules/

# Build output
out/
dist/

# Local model files (never commit large GGUF weights)
*.gguf
/models/
/resources/llama-server/*
!/resources/llama-server/README.md
/resources/windows-control/*
!/resources/windows-control/README.md
/native/windows-control/bin/
/native/windows-control/obj/
# Exception: the small embedding model bundled with Anodex itself (semantic
# code search) ships as part of the app, not a user-downloaded chat model.
!/resources/embedding-model/*.gguf

# Logs
*.log
npm-debug.log*

# Environment / local overrides
.env
.env.local

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Local coding-agent session state, not project source
.claude/

# Local coding-agent session state / visualizations, not project source
.codex/

# TypeScript incremental build cache
*.tsbuildinfo

# Ad-hoc local notes/audits and packaging artifacts dropped in the repo root
/CC/
/scratchpad/
/project-1/
/*.zip

# Local generated UI samples
/docs/ui-samples/

# Test-runner artifacts
/test-results/
