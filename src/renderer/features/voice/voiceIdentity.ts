/**
 * Arc.
 *
 * The voice has a name because it is a thing people will refer to, and because
 * "the voice" stops working the moment there are two. It is written once, here,
 * and matched by `VoiceIdentity.kt` on the phone — the same name in both places
 * or it is two products.
 */
export const VOICE_NAME = 'Arc'
