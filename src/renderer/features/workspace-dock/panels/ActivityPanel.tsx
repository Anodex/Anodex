import { WorkspaceDockPanel } from '../WorkspaceDockPanel'

export function ActivityPanel(): JSX.Element {
  return <WorkspaceDockPanel title="Activity">No active workspace activity.</WorkspaceDockPanel>
}
